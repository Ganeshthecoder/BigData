1. Place the data.txt file in the class path
2. All required validations are implemented in the application
	a. user input validations like 3 and 6 digits integer values for Candidate ID and Voter ID respectively.
	b. user is restricted to input only integer values for the menu options
	c. Prompt the user if he tries to search or count without loading data
3. File not found and other general exceptions are handled in the program
4. The user can dynamically switch between direct addressing and hash table loading without quitting the program. 
5. The find and count will work based on what method of loading is performed and it is dynamic.